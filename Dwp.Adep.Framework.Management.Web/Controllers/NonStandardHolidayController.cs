//------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated from a template.
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------

using System;
using System.Collections;
using System.Collections.Generic;
using System.Collections.ObjectModel;
using System.Collections.Specialized;
using System.Linq;
using System.Web.Mvc;
using System.ComponentModel.DataAnnotations;
using System.ServiceModel;
using Dwp.Adep.Framework.Management.ResourceLibrary;
using AutoMapper;
using Dwp.Adep.Framework.Management.Web.Helpers;
using Dwp.Adep.Framework.Management.Web.ViewModels;
using Dwp.Adep.Framework.Management.Web.Models;
using Dwp.Adep.Framework.Management.Web.AdminService;


namespace Dwp.Adep.Framework.Management.Web.Controllers
{
    public partial class NonStandardHolidayController : BaseController
    {
		#region Edit
		
        // GET: /NonStandardHoliday/Edit
		[CustomAuthorize(Roles = FrameworkRoles.ADMIN)]
        public ActionResult Edit()
        {
			// Retrieve ID from session
			string code = SessionManager.NonStandardHolidayCode;
			
            NonStandardHolidayVM model = new NonStandardHolidayVM();
			
            // Not from staff or error
            if (String.IsNullOrEmpty(code))
            {
                //If session has lists then use them
                RepopulateListsFromCacheSession(model);

                //Assume we are in create mode as no code passed
                model.NonStandardHolidayItem = new NonStandardHolidayModel(){IsActive = true};
            }
            //if we have been passed a code then assume we are in edit situation and we need to retrieve from the database.
            else
            {
				// Create service instance
				AdminServiceClient sc = new AdminServiceClient();
				
                try
                {
					// Call service to get NonStandardHoliday item and any associated lookups    
                    NonStandardHolidayVMDC returnedObject = sc.GetNonStandardHoliday(CurrentUser, CurrentUser, appID, "", code);

					// Close service communication
					sc.Close();

                    //Get view model from service
                    model = ConvertNonStandardHolidayDC(returnedObject);

                    ResolveFieldCodesToFieldNamesUsingLists(model);

                    //Store the service version
                    SessionManager.NonStandardHolidayServiceVersion = model.NonStandardHolidayItem;
                }
				catch (Exception e)
				{
					// Handle the exception
					string message = ExceptionManager.HandleException(e, sc);
					model.Message = message;
					
					return View(model);
				}
            }

            //Adds current retrieved NonStandardHoliday to session
            SessionManager.CurrentNonStandardHoliday = model.NonStandardHolidayItem;
            SetAccessContext(model);
            
            return View(model);
        }    
		
		#endregion
		
		#region Create/Update

        // POST: /NonStandardHoliday/Edit with Create button submitting
		[CustomAuthorize(Roles = FrameworkRoles.ADMIN)]
        [HttpParamEdit(Prefix = "Edit::")]
        [HttpPost]
        public ActionResult CreateNonStandardHoliday(FormCollection collection)
        {
            return UpdateNonStandardHoliday();
        }

        // POST: /NonStandardHoliday/Edit with Save button submitting
        [HttpParamEdit(Prefix = "Edit::")]
        [HttpPost]
        public ActionResult SaveNonStandardHoliday(FormCollection collection)
        {
            return UpdateNonStandardHoliday();
        }

        //This method is shared between create and save
        private ActionResult UpdateNonStandardHoliday()
        {
			// Get the updated model
            var model = GetUpdatedModel();

			// Test to see if there are any errors
            var errors = ModelState
                    .Where(x => x.Value.Errors.Count > 0)
                    .Select(x => new { x.Key, x.Value.Errors[0].ErrorMessage })
                    .ToArray();
            
			//Set flags false
            SetFlagsFalse(model);
			
			// Test to see if the model has validated correctly
            if (ModelState.IsValid)
            {
				// Create service instance
				AdminServiceClient sc = new AdminServiceClient();
				
                //Attempt update
                try
                {
                    // Map model to data contract
                    NonStandardHolidayDC NonStandardHolidayItem = Mapper.Map<NonStandardHolidayDC>(model.NonStandardHolidayItem);

					NonStandardHolidayVMDC returnedObject = null;

                    if (null == model.NonStandardHolidayItem.Code || model.NonStandardHolidayItem.Code == Guid.Empty)
                    {
						// Call service to create new NonStandardHoliday item
                        returnedObject = sc.CreateNonStandardHoliday(CurrentUser, CurrentUser, appID, "", NonStandardHolidayItem);
                    }
                    else
                    {
						// Call service to update NonStandardHoliday item
                        returnedObject = sc.UpdateNonStandardHoliday(CurrentUser, CurrentUser, appID, "", NonStandardHolidayItem);
                    }

					// Close service communication
					sc.Close();
					
					// Retrieve item returned by service
                    var createdNonStandardHoliday = returnedObject.NonStandardHolidayItem;

					// Map data contract to model
                    model.NonStandardHolidayItem = Mapper.Map<NonStandardHolidayModel>(createdNonStandardHoliday);
					
                    //After creation some of the fields are display only so we need the resolved look up nmames
                    ResolveFieldCodesToFieldNamesUsingLists(model);

					// Set access context to Edit mode
                    model.AccessContext = NonStandardHolidayAccessContext.Edit;

					// Save version of item returned by service into session
                    SessionManager.NonStandardHolidayServiceVersion = model.NonStandardHolidayItem;
                    SessionManager.CurrentNonStandardHoliday = model.NonStandardHolidayItem;
                    
                    // Remove the state from the model as these are being populated by the controller and the HTML helpers are being populated with
					// the POSTED values and not the changed ones.
                    ModelState.Clear();
                    model.Message = FixedResources.MESSAGE_UPDATE_SUCCEEDED;
                }
				catch (Exception e)
				{
					// Handle the exception
					string message = ExceptionManager.HandleException(e, sc);
					model.Message = message;
					
					return View(model);
				}
            }

            return View(model);
        }

        #endregion
		
		#region Exit

        // POST: /NonStandardHoliday/Edit with Exit button submitting
		[CustomAuthorize(Roles = FrameworkRoles.ADMIN)]
        [HttpParamEdit(Prefix = "Edit::")]
        [HttpPost]
        public ActionResult ExitNonStandardHoliday(FormCollection collection)
        {
            var model = GetUpdatedModel();
            if (model.IsExitConfirmed == "True" || !model.IsViewDirty)
            {
                //Set flags false
                SetFlagsFalse(model);

                model.IsExitConfirmed = "False";
                
                //remove the current values from session
                SessionManager.CurrentNonStandardHoliday = null;
                SessionManager.NonStandardHolidayServiceVersion = null;

                return RedirectToAction("Search", "NonStandardHoliday");
                
            }
            else
            {
                //Set flags false
                SetFlagsFalse(model);
                model.Message = FixedResources.MESSAGE_EXITCONFIRMATION;
                model.IsExitConfirmed = "True";
            }
            
            return View(model);
        }
		
		#endregion
		
		#region New

        // POST: /NonStandardHoliday/Edit with New button submitting
		[CustomAuthorize(Roles = FrameworkRoles.ADMIN)]
        [HttpParamSearch(Prefix = "Search::")]
        [HttpPost]
        public ActionResult NewNonStandardHoliday(FormCollection collection)
        {
            var model = GetUpdatedModel();
            //Set flags false
            SetFlagsFalse(model);
            
            //Clear Down Session
			SessionManager.NonStandardHolidayCode = null;
            SessionManager.CurrentNonStandardHoliday = null;
            SessionManager.NonStandardHolidayServiceVersion = null;
            
            //Go to the Edit Screen
            return RedirectToAction("Edit", "NonStandardHoliday");
        }
		
		#endregion

		#region Search

        // GET: /NonStandardHoliday/Search
        //This is called when first entering search NonStandardHoliday screen or when paging
		[CustomAuthorize(Roles = FrameworkRoles.ADMIN)]
        public ActionResult Search(int page = 1)
        {   
			// Create service instance
			AdminServiceClient sc = new AdminServiceClient();
			
			// Create model
			NonStandardHolidaySearchVM model = new NonStandardHolidaySearchVM();
			
			try
			{
				NonStandardHolidaySearchVMDC response = sc.SearchNonStandardHoliday(CurrentUser, CurrentUser, appID, "", null, page, PageSize, true);

				// Close service communication
				sc.Close();

				//Map response back to view model
				model.MatchList = Mapper.Map<IEnumerable<NonStandardHolidaySearchMatchDC>, List<NonStandardHolidaySearchMatchModel>>(response.MatchList);

				// Set paging values
				model.TotalRows = response.RecordCount;
				model.PageSize = SessionManager.PageSize;
				model.PageNumber = page;
				
				// Store the page number we were on
				SessionManager.NonStandardHolidayPageNumber = model.PageNumber;
	        
				return View(model);
			}
			catch (Exception e)
			{
				// Handle the exception
				string message = ExceptionManager.HandleException(e, sc);
				model.Message = message;
				
				return View(model);
			}

        }

        // POST: /NonStandardHoliday/Search
        //This is called when clicking search button
		[CustomAuthorize(Roles = FrameworkRoles.ADMIN)]
		[HttpParamSearch(Prefix = "Search::")]
        [HttpPost]
        public ActionResult SearchPost(NonStandardHolidaySearchVM model, int page = 1)
        {
		
			// Iterate through form keys
            foreach (string Key in Request.Form.Keys)
            {
				// Test if Select button was clicked...
                if (Key.StartsWith("Search::SearchPost_"))
                {
					// Retrieve ID for entity which was selected
                    Guid Value = Guid.Parse(Key.Substring(19));
									
					// Store ID for Edit screen
					SessionManager.NonStandardHolidayCode = Value.ToString();
					
					// Call out to Edit screen
                    return RedirectToAction("Edit", "NonStandardHoliday", new { code = Value });

                }
            }

            // Return to the Screen
            return View(model);
        }

        #endregion
		
		#region Private methods
		
        private void SetFlagsFalse(NonStandardHolidayVM model)
        {
            model.IsExitConfirmed = "False";
            model.IsNewConfirmed = "False";

            //Stop the binder resetting the posted values
            ModelState.Remove("IsDeleteConfirmed");
            ModelState.Remove("IsExitConfirmed");
            ModelState.Remove("IsNewConfirmed");
        }

        private void ResolveFieldCodesToFieldNamesUsingLists(NonStandardHolidayVM model)
        {
			//TODO:
        }

        /// <summary>
        /// Private method to merge in the model 
        /// </summary>
        /// <returns></returns>
        private NonStandardHolidayVM GetUpdatedModel()
        {
            NonStandardHolidayVM model = new NonStandardHolidayVM();
            RepopulateListsFromCacheSession(model);
            model.Message = "";

            if (SessionManager.CurrentNonStandardHoliday != null)
            {
                model.NonStandardHolidayItem = SessionManager.CurrentNonStandardHoliday;
            }

            //***************************************NEED WHITE LIST ---- BLACK LIST ------ TO PREVENT OVERPOSTING **************************
            bool result = TryUpdateModel(model);//This also validates and sets ModelState
            //*******************************************************************************************************************************
            if (SessionManager.CurrentNonStandardHoliday != null)
            {
                //*****************************************PREVENT OVER POSTING ATTACKS******************************************************
                //Get the values for read only fields from session
                MergeNewValuesWithOriginal(model.NonStandardHolidayItem);
                //***************************************************************************************************************************
            }

            SetAccessContext(model);

            return model;
        }
		
        private NonStandardHolidayVM ConvertNonStandardHolidayDC(NonStandardHolidayVMDC returnedObject)
        {
            NonStandardHolidayVM model = new NonStandardHolidayVM();
            
			// Map NonStandardHoliday Item
			model.NonStandardHolidayItem = Mapper.Map<NonStandardHolidayDC, NonStandardHolidayModel>(returnedObject.NonStandardHolidayItem);
            
			// Map lookup data lists
        
            return model;
        }
		
		private void RepopulateListsFromCacheSession(NonStandardHolidayVM model)
        {
			// Populate cached lists if they are empty. Will invoke service call
            NonStandardHolidayLookupListsCacheObject CachedLists = CacheManager.NonStandardHolidayListCache;

			// Retrieve any cached lists to model
   
        }

        private void MergeNewValuesWithOriginal(NonStandardHolidayModel modelFromView)
        {
            //***************************The values that are display only will not be posted back so need to get them from session**************************

            NonStandardHolidayModel OriginalValuesFromSession = SessionManager.CurrentNonStandardHoliday;

        }
		
		private void SetAccessContext(NonStandardHolidayVM model)
        {
            //Decide on access context
            if (null == model.NonStandardHolidayItem || model.NonStandardHolidayItem.Code == Guid.Empty)
            {
				// Create context
                model.AccessContext = NonStandardHolidayAccessContext.Create;
            }
            else
            {
				// Edit context
                model.AccessContext = NonStandardHolidayAccessContext.Edit;
            }
        }
		
		private void DetermineIsDirty(NonStandardHolidayVM model)
        {
            //Compare the NonStandardHoliday to the original session
            if (model.NonStandardHolidayItem.PublicInstancePropertiesEqual(SessionManager.NonStandardHolidayServiceVersion, "RowIdentifier"))
            {
                model.IsViewDirty = false;
            }
            else
            {
                model.IsViewDirty = true;
            }

        }
        #endregion
       
    }
}
