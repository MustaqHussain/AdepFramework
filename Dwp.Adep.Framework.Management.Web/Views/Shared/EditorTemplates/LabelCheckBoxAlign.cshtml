@model bool

<div class="row">
    <div class="w40">
        @Html.Label("")
    </div>
    <div class="w60">
    @{
        string tooltip="";
        if (ViewData.ModelMetadata.AdditionalValues.ContainsKey("Tooltip"))
        {
            tooltip = (string)ViewData.ModelMetadata.AdditionalValues["Tooltip"];
            //@Html.CheckBoxFor(x => x,new { value = (Model == null ? "" :  Model.ToString()), @title = tooltip })
            @Html.CheckBoxFor(x => x, new { @title = tooltip })
        }
        else
        {
            //@Html.CheckBoxFor(x => x, new { value = (Model == null ? "" : Model.ToString()) })
            @Html.CheckBoxFor(x=>x)
        }
    }
    </div>
    <div class="row">
        @Html.ValidationMessage("")
    </div>
</div>
